# Copie para backend/.env.prod (não commitar).
# npm run dev-prod no monorepo sobrescreve só o Supabase; o resto vem de backend/.env.
# APP_ENV é forçado para development pelo script (ntfy, e-mail e Stripe outbound não disparam).
# PLATFORM_LOGIN_PASSPHRASE vem do backend/.env se não estiver aqui.

SUPABASE_URL=https://your-prod-project-id.supabase.co
# service_role do projeto de produção. NÃO use a anon key.
SUPABASE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
